apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: cloudflare-gateway-operator-system
spec:
  template:
    spec:
      containers:
        - name: controller
          imagePullPolicy: Never
          image: cloudflare-gateway-operator:latest
